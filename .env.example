# LLM configuration (any OpenAI-compatible API works)
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

INPUT_DIR=./input
OUTPUT_DIR=./organized

# Vision backend: max pages rasterized per PDF (pdftoppm at 150dpi)
MAX_PAGES=20
